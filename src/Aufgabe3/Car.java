package Aufgabe3;

public class Car extends Vehicle{
    public Car(String manufacturer, String model, int weight, int constructionYear, int maxSpeed) {
        super(manufacturer, model, weight, constructionYear, maxSpeed);
    }
}
