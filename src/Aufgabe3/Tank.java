package Aufgabe3;

public class Tank extends Vehicle{
    public Tank(String manufacturer, String model, int weight, int constructionYear, int maxSpeed) {
        super(manufacturer, model, weight, constructionYear, maxSpeed);
    }
}
