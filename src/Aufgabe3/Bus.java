package Aufgabe3;

public class Bus extends Vehicle{
    public Bus(String manufacturer, String model, int weight, int constructionYear, int maxSpeed) {
        super(manufacturer, model, weight, constructionYear, maxSpeed);
    }
}
