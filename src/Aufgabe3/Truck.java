package Aufgabe3;

public class Truck extends Vehicle{
    public Truck(String manufacturer, String model, int weight, int constructionYear, int maxSpeed) {
        super(manufacturer, model, weight, constructionYear, maxSpeed);
    }
}
